# Environment Configuration
base.url=https://www.example.com/shopease

# Browser Configuration (chrome, firefox, edge)
browser=chrome

# Timeouts in seconds
timeout.implicit=10
timeout.explicit=20
timeout.pageLoad=30
